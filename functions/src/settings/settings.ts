export const COLLECTION_PREFIX = 'x-';




/**
 * 
 * Use can set `USE_UID` to true IF you want to test the unit tests with UID instead of ID Token.
 * 
 * This must be false when you deploy.
 * 
 * OR, you can test with real idToken instead of Fake `uid`. So, you don't have to worry about the mistake by forgetting to change `USE_UID` to true when deploy.
 * 
 */
export const USE_UID = true;
